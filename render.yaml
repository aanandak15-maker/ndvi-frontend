services:
  - type: web
    name: ndvi-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python image_server.py"
    envVars:
      - key: PORT
        value: 8000
